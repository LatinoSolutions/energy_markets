# LAT-222 closure candidate v4 — native capability gated callers

This version preserves v1, v2, and v3 evidence and adds the active-descriptor emission gate. `gatherBacklog` re-reads only relevant Codex command/reviewer agents through the existing `GET /agents/:id` route. The descriptor is accepted only with `available=true`, `enabled=true`, version 1, a 64-hex `configSha256`, and a matching profile that authorizes the actor and lane role. Refill, final-acceptance, Command-question, dispatch-review, and stale-reference callers emit `assigneeAccountProfileId`/`accountProfileId` only when that per-lane proof is present. With absent/unavailable metadata they stay in fixed resource/HOME compatibility and emit no binding field. No new route, issue-wide override, agent actor mutation, quota mutation, controller installation, or native service write is included.

The descriptor fields consumed are `available`, `version`, `enabled`, `configSha256`, and `profiles` with `id`, `capacity`, `allowedAgentIds`, `legacyAgentIds`, `allowedRoles`, and optional `legacyDefaultRole`. HOME and secrets are excluded; the native runtime owns per-run HOME and capacity enforcement. The real lane order keeps `command_oa1` before `command_oa2` and `review_ant`, so an independent OA1 reviewer route remains available when OA2 closes and OA1 is open. Go provider Global-region failures have no supported durable staging circuit hook; root's administrative pause is recorded as residual evidence only.

Validation: full JavaScript suite 696/696; focused capability/binding suite passed; Python email sink 7/7. The final readback at 2026-09-21T22:38:39.639Z found LAT-222 blocked with executionState idle and no current participant/return assignee. Live GETs for the Tech Lead and Independent Reviewer were idle and omitted `nativeAccountBinding`, so this candidate makes no live dynamic-binding claim.

The manifest carries a 95-file static staging inventory. Runtime/generated files and preserved `.orig`/`.rej` artifacts are explicitly excluded; the installed-source baseline is referenced by path and SHA-256 only.
