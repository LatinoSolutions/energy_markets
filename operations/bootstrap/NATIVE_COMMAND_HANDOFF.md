# Director handoff: persistent native bootstrap continuation
Owner authorized full Energy implementation on Brunode, 2026-09-19. This is an operational decomposition of the existing SPEC, not a new architectural plan.

After the ST and distinct parent acceptance of IMP-01 and IMP-25, native Command must verify both exact scoped IMP_RECEIPTs, hashes, reviews and §20.2.10 gates. If any gate fails, return changes only on that branch. If both pass and DEP-27 required scope is factually sufficient, the prepared dependent packet LAT-97 (b4f03ad2-e300-4b21-8338-0b04fe4bd389) becomes eligible. It is intentionally WAITING/unassigned now; no early dispatch. Read its full packet and current CONTINUITY_STATUS/resource readings. Command may replace its WAITING title with READY and perform one explicit bootstrap native assignment through existing API to an actually OPEN routine production lane (DeepSeek per existing routing), recording reason and current source evidence. This temporary Director-coordinated bootstrap assignment is necessary because IMP-26 is the work that fixes the existing controller binding. Never assign a CLOSED lane or change routing/quotas. Do not create duplicate packets or a second office.

The coordinator's earlier instruction not to start IMP-26 while it was being prepared is superseded ONLY after these gates pass: the bounded packet is now fully prepared as LAT-97.

For IMP-26: independent Opus review first, then native Command final. Worker cannot modify services. Command in its operational Director role may, only after the independent code-review gate passes, start/enable the SAME existing latinosolutions-continuity.timer and perform bounded live Energy validation on the reviewed binding. Check actual source hashes against review, Project Energy ON/Alexandria OFF, no cross-project tasks/wakes, native receipts and real next eligible work from canonical graph. No new timer/scheduler, no budgets changes, no Alexandria execution, no commercial actions. Actual successful continuation evidence is required before accepting IMP-26; code approval is not final acceptance. Preserve old project state/receipts and dirty concurrent owner changes. If live validation fails, keep only the affected unsafe dispatch path held, record exact evidence and return changes; do not fake PASS. Record services/state changes with timestamps. Parent receipt separate; keep program root LAT-91 open while canonical work remains.

The final automatic loop must remain entirely on Brunode and use current office quotas, recovery and Hermes. When no eligible work exists, record typed blockers; human escalation only for authority actually needed. No invented task to maintain queue size. Follow §§20.2 and25.2; completed engineering is not economic validation. The user is designing frontend separately; no full UI prerequisite.
